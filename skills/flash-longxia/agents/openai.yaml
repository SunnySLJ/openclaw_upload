interface:
  display_name: "Flash LongXia"
  short_description: "Generate one video from up to 4 images, or query/download tasks"
  default_prompt: "Use $flash-longxia to generate one video from 1 to 4 local images in this repo, or query and download a completed video by task ID."

policy:
  allow_implicit_invocation: true
